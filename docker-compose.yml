version: '3.8'

services:
  yt-downloader:
    build: .
    container_name: yt-downloader
    ports:
      - "${APP_PORT:-5000}:5000"
    volumes:
      - ./downloads:/downloads
    environment:
      - FLASK_ENV=${APP_ENV:-development}
    restart: unless-stopped
